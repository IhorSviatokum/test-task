import React from 'react';
import classes from './AddEventButton.module.css'

const addEventButton = props => {
    return (
        <button className={classes.AddEventButton}>+</button>
    )
}

export default addEventButton